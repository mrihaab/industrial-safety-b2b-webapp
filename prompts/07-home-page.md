# 07 — Home / Landing Page Implementation Prompt

## Objective
Build the Home / Landing Page (`/`) with 100% visual fidelity to the `Home.tsx` mockup reference.

## Instructions for AI Agent
1. `HeroBanner.tsx`: Implement dot-grid background (`.industrial-grid`), pulsing compliance badge (`GLOBAL COMPLIANCE ACTIVE`), headline, subtitle, grayscale-to-color hero photo, and dual CTAs (`Explore Catalog` & `View Certifications`).
2. `StatsBar.tsx`: Implement 4-column metrics bar (`45+ Countries Reached`, `12M Units Shipped`, `100% CE Compliance`, `24/7 Safety Monitoring`) with left border orange accents.
3. `FeaturedBento.tsx`: Build 12-column asymmetric product bento grid:
   - Titan-X Safety System (Main 6-col 2-row hero card with `NEW RELEASE` tag & `View Specs` CTA).
   - Pro-Vis Series (6-col 1-row apparel card).
   - IronStride Boots (3-col 1-row footwear card with backdrop-blur sliding drawer).
   - Custom Fitting (3-col 1-row service promo card with `settings_input_component` icon).
4. `ExportMap.tsx`: Implement 2-column global logistics section featuring glowing 3D routes map and supply chain bullet features (`verified`, `speed`, `support_agent`).
5. `CertGrid.tsx`: Implement 6-column lab certification emblem bar (`ISO 9001`, `CE MARKED`, `ANSI / ISEA`, `OSHA READY`, `REACH COMPLIANT`, `UKCA`) with hover grayscale effect.
6. `SalesBanner.tsx`: Implement solid safety orange sales CTA banner (`bg-primary-container`), semi-transparent email input, and contrast `CONTACT SALES` button.

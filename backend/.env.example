PORT=5000
NODE_ENV=production

# MySQL Production Configuration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=your_production_mysql_password
DB_NAME=ghulam_safety_hub

# JWT Security (Mandatory in Production - Must be a strong random string)
JWT_SECRET=your_production_high_entropy_jwt_secret_key_here

# Production Frontend CORS Whitelist
CORS_ORIGIN=https://industrial-safety-b2b-webapp.vercel.app

# SMTP Email Configuration (Optional - fallback to log dispatch if empty)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=bulk@ghulamsafety.com
SMTP_PASS=your_app_specific_smtp_password

# WhatsApp Helpline
WHATSAPP_PHONE=97145550192
